#pragma once

#include "Update.hpp"

//----------------------------------------------------------------------------//
// UpdateSystem
//----------------------------------------------------------------------------//

//----------------------------------------------------------------------------//
UpdateSystem::UpdateSystem(Scene* _scene) :
	SceneSystem(_scene)
{
}
//----------------------------------------------------------------------------//
UpdateSystem::~UpdateSystem(void)
{
	// ...
}
//----------------------------------------------------------------------------//
void UpdateSystem::Update(float _dt)
{
	/*for (UpdateableComponent* i = m_active; i; i = i->m_nextUpdateable)
	{
		if (i->IsFixedUpdate())
		{
			for(float _t = 0; _t < _dt;)
			{
				float _step = 1.f / 60;
				_t += _step;
				if(_t > _dt)
					_step = _t - _dt;
				i->Update(_step);
			}
		}
		else
		{
			i->Update(_dt);
		}
	}*/
}
//----------------------------------------------------------------------------//

//----------------------------------------------------------------------------//
//
//----------------------------------------------------------------------------//
